{{!-- Широкая полоса-передышка (страницы.md §1). Между «как растёт стул» и безопасностью:
      двенадцать секций подряд одного размера читаются монотонно, и глазу нужно место,
      где ничего не надо делать. Кадр во всю ширину, без текста и без ссылки.
      Горизонтального снимка у бренда пока нет — стоит плашка нужных пропорций. --}}
<section aria-hidden="true">
  {{> picture empty id="home-atmosfera" alt=""
      ratio="aspect-video sm:aspect-panorama" sizes="100vw"
      class="band-screen aspect-video w-full object-cover sm:aspect-panorama"}}
</section>
