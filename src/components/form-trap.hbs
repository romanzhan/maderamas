{{!-- Ловушка для ботов (формы-и-поля.md п. 5): поле скрыто, но не убрано из потока —
      display:none его выдаёт. Заполнено — отправка «удалась» и не сделала ничего,
      чтобы бот не узнал, что его отсеяли. Поле проверяет и сервер — на чекауте
      и у всех форм обратной связи (бэкенд.md §7, §14).
      Подписи у поля нет намеренно: человеку оно не показывается и скринридером не
      читается, а испанских строк в разметке не бывает (принцип 27). Бот находит поле
      по имени, а не по подписи. --}}
<div class="sr-only" aria-hidden="true">
  <input type="text" name="website" tabindex="-1" autocomplete="off" />
</div>
