# Контекст проекта

> Живой файл. Тут — то, что не лежит в коде и не выводится из git log:
> решения, их причины, текущее состояние серверов и открытые задачи.
> Обновляется по ходу работы. Если факт устарел — правьте, не дописывайте рядом.
>
> Постоянные правила разработки — в `CLAUDE.md`. Здесь — статус и история решений.

Обновлено: 2026-08-17.

## О проекте

Интернет-магазин детской мебели, рынок Аргентины. Переезжает с Tiendanube
(`maderamas.com.ar` сейчас резолвится на 185.133.35.13/14 — это Tiendanube,
хостится в Бразилии). **Tiendanube не отключать**, пока новый магазин не
пройдёт проверку реальными заказами — это единственный источник дохода на
время миграции.

Каталог маленький: **8 товаров**, известны из старого магазина. Атрибуты
(глобальные, WooCommerce) видны из фильтров Tiendanube:
- `pa_color` — реальные значения включают Blanco
- `pa_madera` — реальные значения включают Roble Americano, Nogal

⚠️ В `docs/content-guide.md` сейчас в таблице атрибутов **примеры-заглушки**
(`natural, blanco, miel` / `pino, paraíso, guatambú`) — их надо свести с
реальными значениями из Tiendanube перед наполнением каталога (Фаза 2).

Фазы (из `docs/plugins.md`, `docs/content-guide.md`):
1. Вёрстка/дизайн — **заблокирована**, ждём референсы от заказчика.
2. Структура каталога и контент.
3. —
4. Mercado Pago (боевые ключи).
5. SEO-плагин, кэш при необходимости.

## Хостинг: решение и почему

Коротко: **шаредхостинг Hostinger Premium, Сан-Паулу**, не VPS.

Ход рассуждения (сворачивать не стоит — пригодится, если придётся объяснять
заказчику или пересматривать):

- Изначально планировали VPS (Ubuntu 24.04 + CloudPanel) — казалось, что
  нужен root и SSH под rsync-деплой.
- Сравнили провайдеров с VPS: Hostinger (Сан-Паулу, известный, дёшево, но
  Аргентины у них нет), Vultr (Сан-Паулу, надёжно, руками больше), DonWeb
  (Аргентина, локальная задержка и factura, но контора меньше и скромнее
  инструментарий), AWS Local Zone Buenos Aires (низкая задержка, но дорого
  и избыточно для 8 товаров).
- Решающий факт: нынешний Tiendanube-магазин **уже** отдаётся из Бразилии
  (185.133.35.x зарегистрированы в BR) — переезд в Сан-Паулу ничего не
  ухудшает по сравнению с тем, что есть у покупателей сегодня. С Cloudflare
  впереди локация origin вообще перестаёт быть заметна.
- Потом проверили факт, который отменил решение о VPS: на шаред-планах
  Hostinger **есть SSH** (rsync тоже поддерживают официально) — но
  **только на Web Premium, Web Business и Cloud**, не на самом дешёвом
  тарифе. Для магазина на 8 товаров VPS оказался избыточен — купили
  Premium.
- Куплен план **Web Premium** (~13 000 ARS/мес на момент покупки — цена
  продления обычно заметно выше витринной, Hostinger обычно завлекает
  ценой за предоплату 24–48 месяцев).

Что это даёт и чего не даёт:
- Есть: SSH (порт **65002**, нестандартный — уже зашит в
  `.github/workflows/deploy.yml` через переменную `DEPLOY_PORT`), WP-CLI,
  выбор версии PHP в hPanel.
- Нет: root, тонкая настройка PHP-FPM/OPcache, обычно нет Redis. Для
  8 товаров не критично.
- На одном Premium/Business-плане можно держать **несколько сайтов** —
  `dev.maderamas.com.ar` и потом сам `maderamas.com.ar` можно на одной
  подписке, второй раз платить не придётся. Оговорка: сайты делят
  ресурсы и аккаунт.
- `proc_open`/`proc_close` в PHP отключены — часть wp-cli команд не
  работает (`wp --info`, автопроверка в `wp rewrite structure --hard`),
  но сам wp-cli и деплой это не блокирует, проверено на практике.

## Текущее состояние (staging, по факту на 2026-07-31 ночь)

✅ **Всё работает, и теперь без костылей.** Боевой Tiendanube-магазин на
`maderamas.com.ar`/`www`, staging на `dev.maderamas.com.ar` — сайт в
hPanel правильно привязан именно к `dev`, апекс в хостинг-аккаунте
больше не занят под сайт (только DNS). Дошли сюда через два инцидента
подряд (подробности — в блоке ниже) — стоит прочитать перед следующими
правками DNS/домена на Hostinger.

**DNS управляется в Hostinger** (NS домена делегированы туда из nic.ar,
регистратор остаётся nic.ar — трансфер `.ar`-домена в Hostinger как
регистратора невозможен в принципе).

Итоговая раскладка DNS-зоны `maderamas.com.ar`:
- apex `@` — A → `185.133.35.13`, `185.133.35.14` (Tiendanube, боевой)
- `www` — CNAME → `wwwmaderamascomar.mitiendanube.com` (Tiendanube, боевой)
- `dev` — обслуживается автоматически, т.к. `dev.maderamas.com.ar` —
  это теперь и есть домен сайта в hPanel (не parked, не поддомен)
- autodiscover/autoconfig на mail.hostinger.com остались от одной из
  промежуточных версий зоны — почтой не пользуемся, не мешают, можно
  почистить при желании

Аккаунт `u489565363`, IP `185.211.7.151`, SSH-порт `65002`. Путь до
сайта: `/home/u489565363/domains/dev.maderamas.com.ar/public_html`.

### Инциденты 2026-07-31: два падения боевого магазина при настройке DNS/домена

**Инцидент 1 (при делегировании NS на Hostinger).** hPanel
интерпретировал добавление домена `maderamas.com.ar` как «сайт» —
привязал WordPress-инсталляцию (до этого жившую в `dev.maderamas.com.ar`)
к апексу, переименовав папку. Когда NS переключились, Hostinger сам
сгенерировал дефолтную DNS-зону для этого «сайта» — apex/`www` стали
смотреть на инфраструктуру Hostinger вместо Tiendanube. Починили через
Hostinger API: удалили ошибочные записи, восстановили `@`/`www` →
Tiendanube. Для `dev` тогда сделали **parked domain** поверх апекса
(рабочий, но кривой промежуточный вариант — апекс физически «сайт»,
а нужный домен — алиас).

**Инцидент 2 (попытка сделать по-человечески, без костыля).** Чтобы
`dev.maderamas.com.ar` стал самим сайтом, а не parked domain, сделали
в hPanel **Change domain**: `maderamas.com.ar` → `dev.maderamas.com.ar`
(предварительно убрав parked domain, чтобы не было конфликта имён).
Файлы и база пережили операцию штатно (как и обещано в документации
Hostinger) — но **вся DNS-зона `maderamas.com.ar` при этом удалилась
целиком** (apex начал отдавать NXDOMAIN), потому что Hostinger, похоже,
привязывает жизненный цикл DNS-зоны к тому домену, который сейчас
числится доменом сайта, а не к домену как таковому. Магазин снова лёг.

Починили тем же способом: через API добавили `maderamas.com.ar` как
**parked domain** сайта `dev.maderamas.com.ar` (это пересоздало зону),
удалили автосгенерированные Hostinger-записи (A на их IP, самозамкнутый
CNAME `www`), прописали `@`/`www` → Tiendanube заново. Заодно поправили
`AuthUserFile` в `.htaccess` (путь снова переехал) и `DEPLOY_PATH` в
GitHub Secrets. `siteurl`/`home` не трогали — были уже правильные.

**Итоговая структура (то, что описано в разделе выше) — сайт в hPanel
привязан к `dev.maderamas.com.ar`, апекс — parked domain того же сайта**,
но с DNS, вручную указывающим на Tiendanube. По ощущениям хостинга это
всё ещё «apex как бы принадлежит хостинг-аккаунту» — то, чего хотелось
избежать, — но технически это единственная конфигурация в рамках
API/hPanel Hostinger, которая: (а) держит реальный сайт под `dev`,
(б) не роняет DNS-зону апекса, (в) не пересоздаёт её каждый раз при
правках. Разделить домены полностью (apex вообще не связан ни с каким
сайтом в аккаунте) через доступный API/hPanel не получилось — либо
сайт «занимает» домен целиком, либо домен не имеет DNS-зоны вовсе
(ошибка `[DNS:4009] Domain not found`).

**Урок на будущее:** любая операция в hPanel/API Hostinger, меняющая
привязку домена к сайту (`Add website`, `Change domain`), затрагивает
DNS-зону этого домена целиком — непредсказуемо и без явного
предупреждения. Перед такими операциями: **снять `curl`/`nslookup`
слепок текущих записей** (как в этом файле) и держать наготове рестор.
Hostinger API (`developers.hostinger.com`, токен в hPanel → Advanced →
API, см. `CREDENTIALS.local.md`) — быстрее и безопаснее ручных кликов
в hPanel для восстановления: снятие/починка каждый раз занимали минуты.

Сделано:
- WordPress 7.0.2 (был уже установлен мастером Hostinger, версия
  совпала с `.wp-env.json` — переустановки не потребовалось).
- WooCommerce 10.9.4 + Mercado Pago 8.9.0 — версии как в
  `.wp-env.json`/`docs/plugins.md`.
- `wp-config.php`: `WP_ENVIRONMENT_TYPE=staging`, `DISALLOW_FILE_EDIT=true`.
  `DISALLOW_FILE_MODS` сознательно **не** ставили — может помешать
  собственным автообновлениям хостинг-плагинов Hostinger.
- Аргентинские настройки WooCommerce (ARS, AR:C, `/productos/` и т.д.).
- Тема `maderamas` задеплоена и активирована, дефолтные темы удалены.
- `mu-plugins` Hostinger (`hostinger-auto-updates.php`,
  `hostinger-preview-domain.php`) не тронуты — деплой синхронизирует
  в `mu-plugins` только файлы с префиксом `maderamas*` (см. #4 ниже).
- Автодеплой работает: push в `main` → GitHub Actions → rsync на
  staging. Секреты/переменные в GitHub (environment `staging`):
  `DEPLOY_HOST`, `DEPLOY_USER`, `DEPLOY_PATH`, `DEPLOY_SSH_KEY`,
  `DEPLOY_PORT=65002`, `DEPLOY_ENABLED=true`.
- `noindex` работает (проверено: `<meta name='robots' content='noindex, nofollow' />`).
- Админка на русском лично для администратора (`user_meta.locale=ru_RU`),
  сайт для покупателей остаётся `es_AR`.
- **Basic Auth работает** (проверено живьём: без пароля — 401 и на origin,
  и через `hcdn`; с паролем `maderamas`/см. `CREDENTIALS.local.md` — 200,
  отдаёт реальную тему `maderamas`). Реализация и грабли — в
  `docs/deploy.md`. Актуальный путь `AuthUserFile` и `.htpasswd` —
  `/home/u489565363/domains/dev.maderamas.com.ar/.htpasswd` (вне webroot).
- `wp option siteurl`/`home` = `https://dev.maderamas.com.ar` (актуальный
  публичный адрес staging).
- `DEPLOY_PATH` в GitHub Secrets = `.../domains/dev.maderamas.com.ar/public_html/wp-content`.
- **WP-автообновления уже отключены** штатно — мu-plugin Hostinger
  (`hostinger-auto-updates.php`) сам ставит
  `automatic_updater_disabled=true` и `auto_update_theme/plugin=false`.
  Отдельный переключатель в hPanel (см. п.3 открытых задач) — это другой,
  серверный механизм Hostinger вне WP, его надо проверить отдельно.

Баги, найденные и починенные по пути (см. PR #4, #5+#6):
- rsync `--delete` по всей `mu-plugins` чуть не затёр файлы Hostinger —
  пофикшено `--include='maderamas*' --exclude='*'`.
- Секрет `DEPLOY_PATH` ломался при установке из Git Bash на Windows
  (MSYS сам конвертирует `/home/...` в путь Windows) — чинится
  `MSYS_NO_PATHCONV=1 gh secret set ...`. `--old-args` для rsync
  оказался не нужен, это была неверная гипотеза (см. #5 → revert #6).
- Basic Auth через `<Files>`-исключение для `admin-ajax.php`/`wp-cron.php`
  не сработал (похоже на `AuthMerging`, сливающий правила вместо
  переопределения) — пофикшено одним `<FilesMatch>` с отрицательным regex.
  `.htpasswd` с правами `640` не читался LiteSpeed-воркером (другой
  пользователь) — фикс `chmod 644`.

## Открытые задачи — что осталось сделать руками (доступ только у вас)

Подтверждено (2026-08-17): все организационные/инфраструктурные задачи по
staging закрыты. Референсы дизайна получены в тот же день (см. «Источники
данных» ниже) — блокеров для старта больше нет. Переходим к прямому
построению сайта на `dev.maderamas.com.ar`.

6. Прод (`www.maderamas.com.ar`) не трогаем, пока staging не обкатан.

Закрыто (подтверждено 2026-08-17):
- ~~DNS~~ — сделано (2026-07-31), подробности в разделе «Хостинг» выше.
- ~~Почистить CDN-кэш Hostinger~~.
- ~~Проверить автообновления в hPanel~~.
- ~~Решить судьбу плагинов `hostinger-easy-onboarding`/`hostinger-reach`~~.
- ~~Наполнение каталога~~ (сведение атрибутов `pa_color`/`pa_madera`
  с `docs/content-guide.md`).

## Следующий этап: построение сайта на dev

Активная фаза — предметная работа на `dev.maderamas.com.ar` (дизайн главной,
каталог, карточка товара, наполнение). Зафиксировано 2026-08-17: **работа
по коду не начата** — сначала нужно собрать и свести источники данных.
Ниже — план и то, что запрошено у заказчика/пользователя.

### Источники данных

Получены от пользователя 2026-08-17, ссылки — в `CREDENTIALS.local.md`
(вне git, доступ на просмотр/редактирование чужих ресурсов туда не
коммитим). Открываемость каждой проверена живьём в браузере:

1. **Mercado Libre** — страница продавца «Madera Más» (+130 подписчиков),
   каталог по категориям (Juegos y Juguetes, Bebés, Hogar/Muebles/Jardín).
   Открывается, товары/цены видны без логина.
2. **Word-документ «ассортимент» от заказчицы** (Google Docs) —
   открывается, доступ уже есть (не требует отдельного запроса прав).
   Внутри — карточки товаров: название, цена, материал, размеры,
   возрастной диапазон, нагрузка, фото. Формат по позициям однороден —
   хорошо ляжет в парсинг.
3. **Google Drive, папка «Фотосессия»** — открывается, доступ есть.
   На верхнем уровне 7 подпапок: `Горка`, `Silla Alta de Comer` (×2,
   разные отделки), `Silla bebe (Pino)`, `Silla bebe (Roble...)`,
   `Silla Evolutiva Infantil` (×2) — то есть частичная рассортировка,
   про которую предупреждал пользователь, подтвердилась: разбито по
   товару/отделке, но не факт что 1:1 с финальным ассортиментом сайта.
4. **Референс-сайт** (`roststul.ru`) — открывается. Растущие стулья
   («Кузя»/Pavlin), RU-рынок, каталог с карточками (фото, цена, скидки,
   быстрое добавление), галерея отзывов, блог. Минималистичный стиль,
   нейтральная бело-серая база + древесные тона — ориентир по структуре
   и общему настроению, не по языку/рынку (сайт русскоязычный, нам
   нужен es-AR).
5. **Референс палитры / главной страницы** — оказался готовым
   brand-гайдом (артефакт Claude «Branding system Madera Más», мой же,
   доступ подтверждён), не просто картинкой. Разделы: Логотип, Цвет,
   Типография, Применения. Логотип — абстрактная «M» на срезах/волокнах
   дерева, тёплый чёрный + акцентный терракотовый в слове «más». Гайд на
   русском (для нас), тексты в макетах — на испанском (то, что увидит
   покупатель). Это существенно снимает риск разночтений по цвету —
   токены `theme.json` можно будет взять оттуда напрямую.

Уже было и отдельно не запрашивалось:
- Старый магазин на Tiendanube (`maderamas.com.ar`/`www`) — источник
  фото/текстов/описаний, спарсим напрямую с боевого сайта.
- SSH/WP-CLI доступ на Hostinger — см. «Хостинг» выше и
  `CREDENTIALS.local.md`.

**Все источники доступны, работа по сбору/сверке данных и вёрстке
может начинаться.**

### План (после получения источников)

1. **Сбор и парсинг.** Собрать по каждой из 8 позиций (`docs/content-guide.md`)
   данные из всех источников: Tiendanube (фото, тексты, описания),
   Mercado Libre (цены, описания), Word-документ, альбом с фотосессией.
   Свести в единую рабочую таблицу вне git (сырые данные заказчика —
   не в репозиторий).
2. **Сверка и ревью.** На пересечениях источников — разночтения в ценах,
   описаниях, атрибутах (`pa_color`, `pa_madera`) — фиксировать и уточнять
   у пользователя, а не гадать. Свести с заглушками в
   `docs/content-guide.md` (см. открытую сверку атрибутов).
3. **Дизайн главной страницы.** По утверждённой палитре и референс-сайту,
   в рамках правил `CLAUDE.md` (design-токены в `theme.json`, блочная
   тема, никаких page builder'ов).
4. **Вёрстка страниц.** Главная, каталог, карточка товара — в теме
   `wp-content/themes/maderamas`.
5. **Наполнение WooCommerce.** Товары, атрибуты, фото (через WP-медиа —
   `uploads` не деплоится, см. `CLAUDE.md`), переводы через
   `Maderamas_Lang` (один пост на языки, без дублирования).
6. **Локализация и рынок.** Тексты — es-AR (аргентинский испанский,
   регистр voseo/лексика рынка), не нейтральный/европейский испанский.
7. **Проверка.** `npm run lint`, визуальная проверка в браузере
   (главная, каталог, карточка) перед тем, как звать заказчика на ревью
   для дальнейших правок.

Дальше заказчик подключается предметно — правки уже по месту.

## Где что искать

- `docs/deploy.md` — деплой и первичная настройка сервера, точные команды.
- `docs/plugins.md`, `.wp-env.json` — зафиксированные версии ядра/плагинов.
- `docs/content-guide.md` — структура каталога, атрибуты, товары под заказ.
- `docs/decisions/` — ADR (почему WooCommerce, почему свой i18n, почему блочная тема).
- `bin/bootstrap-server.sh` — идемпотентный скрипт настройки сервера.
