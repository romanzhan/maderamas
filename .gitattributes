# Окончания строк не переписываются автоматически: файлы промта пришли смешанными,
# и перегенерация целого файла ради них прячет настоящую правку в тысяче фантомных
# строк — а диф перед коммитом читается глазами (принцип 40).
* -text
